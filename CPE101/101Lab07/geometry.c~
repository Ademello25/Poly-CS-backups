#include "geometry.h"

double distance(Point p1, Point p2)
{
   return sqrt(((p2.x - p1.x) * (p2.x - p1.x)) + ((p2.y - p1.y) * (p2.y - p1.y)));
}
int area(Rectangle A)
{
   return abs((A.p2.x - A.p1.x) * (A.p2.y - A.p1.y));
}
